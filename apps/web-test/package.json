{
  "name": "@autofill/web-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@autofill/core": "0.1.0",
    "@autofill/shared": "0.1.0",
    "@autofill/ui": "0.1.0",
    "@autofill/workers": "0.1.0"
  }
}
